<% if logged_in? %>
  <script type="text/javascript">
    const user = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>

    window.currentUser = user.user;
    window.events = user.events;
  </script>
<% end %>
<div id="root">
</div>